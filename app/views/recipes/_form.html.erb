<%= fields_for @recipe do |f| %>
  <%= f.label :recipe_name %>
  <%= f.text_field :recipe_name %>
  <%= f.collection_select(:ingredients, Ingredient.all, :id, :ingredient_name) %>
  <%= f.submit %>
  
<% end %>
